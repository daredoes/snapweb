import { styled } from "@mui/material";

export const Divider = styled("hr")(() => {
  return {
    width: "90%",
  };
});

export const StreamImg = styled("img")(() => {
  return {
    height: "25px",
    width: "auto",
  };
});
