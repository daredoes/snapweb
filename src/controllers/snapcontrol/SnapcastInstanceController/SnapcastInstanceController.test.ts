/// <reference lib="dom" />
import { test, expect } from "bun:test";
import SnapcastInstanceController from "./SnapcastInstanceController";

test.todo("dom test", () => {
  const controller = SnapcastInstanceController.getInstance();
  // expect(controller).toEqual("none");
});
